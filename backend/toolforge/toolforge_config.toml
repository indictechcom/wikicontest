GREETING = "Welcome to WikiContest on Toolforge!"

# Security
SECRET_KEY = "your-secret-key-here"
JWT_SECRET_KEY = "your-jwt-secret-key-here"

# Database (MySQL for Toolforge - Recommended)
SQLALCHEMY_DATABASE_URI = "mysql://username:password@tools-db/database_name"
SQLALCHEMY_TRACK_MODIFICATIONS = false

# JWT Configuration
JWT_ACCESS_TOKEN_EXPIRES = 86400

# Debug mode (set to false for production)
DEBUG = false

# OAuth Configuration (add your OAuth consumer details)
OAUTH_MWURI = "https://meta.wikimedia.org/w/index.php"
CONSUMER_KEY = "3f383c834a07a181723f1a1de566f7cf"
CONSUMER_SECRET = "62c40e0fde2377613d1f82b9b7aabc9fe2a73b30"

# OAuth Callback Path
# This must match the callback URL registered in your OAuth consumer
# For Toolforge, the OAuth consumer is registered with /oauth/callback
# 
# IMPORTANT: This setting tells the code to use /oauth/callback instead of 
# the default /api/user/oauth/callback. The code will build:
# https://wikicontest.toolforge.org/oauth/callback
OAUTH_CALLBACK_PATH = "/oauth/callback"

# OAuth Callback Type
# Set to true if OAuth consumer was registered with "oob" (out-of-band)
# Set to false if registered with a callback URL (recommended for web apps)
OAUTH_USE_OOB = false